// import pcl_0800 from "monday.json" assert { type: 'json' }; 

// Variables for each time of day
var pcl_0800, pcl_0900, pcl_1000, pcl_1100, pcl_1200, pcl_1300, pcl_1400, pcl_1500, pcl_1600, pcl_1700, pcl_1800, pcl_1900, pcl_2000, pcl_2100, pcl_2200, pcl_2300;

// Fetch the JSON file
fetch(day = "monday.json")
  .then(response => response.json())
  .then(data => {
    // Access the parsed JSON data
    pcl_0800 = JSON.parse(data.pcl_0800);
    pcl_0900 = JSON.parse(data.pcl_0900);
    pcl_1000 = JSON.parse(data.pcl_1000);
    pcl_1100 = JSON.parse(data.pcl_1100);
    pcl_1200 = JSON.parse(data.pcl_1200);
    pcl_1300 = JSON.parse(data.pcl_1300);
    pcl_1400 = JSON.parse(data.pcl_1400);
    // etc. 
    
  })
  .catch(error => {
    console.error('Error fetching or parsing JSON:', error);
  });

// var day = new Date();
//this is what wix generates
$w.onClick(function() {
  
});

function displayInfo() {
  //find what area was moused over / clicked
  //find what day & what time to look at
  //get stats for that area at that day/time
  //(by calling fetch)
  //display stats
} 

// Display a given floor/space
function showArea() {
  
}



